# 예울마루 STAY 패키지 — 객실 선택

문자로 링크를 받은 예매자가 폰에서 **휴대폰 뒷 4자리 확인 → 객실 형태 선택**까지 하는 프로그램.
숙박일은 고객이 고르지 않는다 — **예매한 공연 날짜가 곧 숙박일**이고, 명단에 미리 박혀 있다.

## 운영자가 물어보면 이렇게 답한다

**"객실 몇 개 남았어?" / "현황 알려줘" 류의 질문이 오면 바로 아래를 실행하고 결과를 보여준다.**
관리자 GUI를 열라고 안내하지 말고, 이 명령의 출력을 그대로 전달한다.

```bash
node tools/rooms.mjs              # 남은 객실 (공연별 · 더블/트윈)
node tools/rooms.mjs list         # 선택 완료한 사람 목록
node tools/rooms.mjs pending      # 아직 안 고른 사람 (재안내 대상)
node tools/rooms.mjs all          # 위 셋 한 번에
node tools/rooms.mjs who 1234     # 한 사람 조회
node tools/rooms.mjs cancel 1234  # 선택 취소 (자리 즉시 재개방)
node tools/rooms.mjs csv          # 탭 구분 출력(엑셀 붙여넣기)
```

**"○○○ 추가해줘" / "○○○ 빼줘" / "○○○ 날짜 바꿔줘" 류의 요청도 바로 아래를 실행한다.**
명단을 통째로 다시 받지 마라 — 아래 명령이 나머지는 안 건드린다.

```bash
node tools/rooms.mjs add 1234 b 홍길동   # 한 명 추가 (같은 뒷4자리면 수정)
node tools/rooms.mjs move 1234 c         # 공연(=숙박일)만 바꾸기
node tools/rooms.mjs rm 1234             # 명단에서 빼기
node tools/rooms.mjs roster 명단.txt      # 명단 통째 교체 (전체를 갈아엎을 때만)
```

운영자가 휴대폰 번호를 통째로 주면(예: `01030214650`) **뒷 4자리만** 쓴다 — `4650`.
공연을 이름으로 말하면 구분 코드로 바꿔 쓴다: **a=조재혁 피아노 리사이틀(9/12) · b=트리플 빌(10/14) · c=피아노 피아노(10/27)**.
공연 구분을 안 알려주면 임의로 정하지 말고 어느 공연인지 물어라(그게 곧 그 사람의 숙박일이다).
이미 객실을 고른 사람의 공연을 바꿨으면 `cancel` 도 해야 다시 고를 수 있다 — CLI가 그때 경고한다.

### 접속 — 주소는 묻지 마라, 필요한 건 비번 하나다

실서비스 주소 `https://staypackage.pages.dev` 는 CLI에 기본값으로 박혀 있다. 관리자 비번은 이 순서로 찾는다:

1. 환경 변수 `BOOK_PIN` — claude.ai/code 환경 설정(환경 변수)에 넣어 두면 모든 세션에서 자동으로 잡힌다.
2. 저장소 루트 `.booking.json` → `{ "pin": "<비번>" }` (git에 안 올라간다)
3. 둘 다 없으면 **비번 하나만** 물어라. 받으면 `.booking.json` 에 적어 두고 즉시 실행한다 — 주소나 명단 파일을 되묻지 마라.

GitHub 저장소의 Actions 시크릿(예: `MANAGE_PW`)은 세션으로 전달되지 않는다 — 거기서 찾으려 하지 마라.
이 저장소는 **공개**라서 Actions 워크플로 로그에 명단(이름·뒷4자리)을 찍는 우회도 만들면 안 된다.

배포 전이라면 로컬로 확인할 수 있다 — 창 하나에 `node tools/dev.mjs`, 다른 창에 `node tools/rooms.mjs --local`.

## 파일 지도

| 파일 | 역할 |
|---|---|
| `public/index.html` | 고객 화면 3단계(본인 확인 → 객실 선택 → 완료) |
| `public/admin.html` | 관리자 화면(명단·공연/날짜·수량·현황·취소) |
| `functions/api/book.js` | 서버 전부. 진입점은 `onRequestPost` 하나, `op` 로 갈린다 |
| `tools/rooms.mjs` | 관리자 CLI — 위 명령들 |
| `tools/dev.mjs` | 로컬 미리보기 서버(R2를 메모리로 흉내) |
| `test.mjs` | 검증 58축. **서버를 고쳤으면 58/58이어야 한다** |
| `docs/인수인계.md` | 계약·실측·다음 후보 |
| `docs/관리자안내.md` | 운영자용 안내문(명단·문자 문구·상황별 대응) |

## 고치기 전에 반드시 읽을 것 — 깨면 안 되는 계약 3종

`docs/인수인계.md` §4에 전문이 있다. 요약:

1. **자리 차감 = 파일 1개 + 사후 양보** (`reserve()`)
   재고 총량을 한 파일에 적고 빼지 않는다. 각자 자기 번호 이름의 파일을 만들고, 쓴 뒤 전건을 다시 세어
   정원 안에 드는지 확인한다. 밀렸으면 자기 파일을 지우고 마감으로 답한다.
   **단 쓰자마자 세면 안 된다** — 남의 파일이 아직 안 보이면 정원을 넘긴다(실측: 4자리에 6명 → 5명 성공).
   `settleList()` 로 목록이 잠잠해진 뒤에 센다(`BOOK_SETTLE_MS` 기본 250ms).
   정렬 기준 `(시각, 뒷4자리)` · 사후 재확인 블록 · `settleList()` 를 **지우지 마라**. `test.mjs` [5]가 잡는다.
2. **재고는 저장하지 않는다** — 매번 `정원 − 실제 파일 수`로 계산한다(`leftOf()`). 캐시하지 마라.
3. **관리자는 `BOOK_PIN` 이 없으면 통째로 잠긴다** (`adminOk()`). 기본 비번 폴백을 넣지 마라.

## 손대고 나서

```bash
node test.mjs        # 58/58 이어야 한다
```
화면을 만졌으면 `node tools/dev.mjs` 로 띄워 폰 폭(390)에서 확인한다.
